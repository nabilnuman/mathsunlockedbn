NEXT_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-public-key

# --- Web Push (optional) -------------------------------------------------
# Same VAPID keypair the send-push Edge Function uses. Generate once with
#   npx web-push generate-vapid-keys
NEXT_PUBLIC_VAPID_PUBLIC_KEY=

# --- Daily reminder cron (Vercel Cron → /api/cron/reminders) -----------
# Only needed in the Vercel project, not for local dev.
CRON_SECRET=
SUPABASE_SERVICE_ROLE_KEY=
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:nabilnuuman@gmail.com
